// Cell: cell
/*
{
  "id" : "cell",
  "sections" : [ {
    "name" : "soma",
    "id" : "soma",
    "points3d" : [ "0.0, 0.0, 0.0, 10.0", "0.0, 0.0, 3.1831, 10.0" ]
  } ],
  "groups" : [ {
    "name" : "soma_group",
    "groups" : [ ],
    "sections" : [ "soma" ]
  }, {
    "name" : "all",
    "sections" : [ "soma" ]
  } ],
  "specificCapacitance" : [ {
    "group" : "all",
    "value" : "1.3000001"
  } ],
  "initMembPotential" : [ {
    "group" : "all",
    "value" : "-70.0"
  } ],
  "resistivity" : [ {
    "group" : "all",
    "value" : "20000.0"
  } ],
  "channelDensity" : [ {
    "id" : "Ik_all",
    "ionChannel" : "kd",
    "ion" : "k",
    "group" : "all",
    "condDens" : "0.0",
    "erev" : "-60.0"
  } ],
  "species" : [ ]
}
*/

load_file("subiter.hoc")

begintemplate cell

public init, topol, basic_shape, subsets, geom, memb
public x, y, z, position


//  Some methods for referencing the cells

public toString, netInfo, set_initial_v, set_initial_ion_properties, synlist


// Section: soma, id: soma, parent: $s.parent
create soma
public soma

// Group: {name=soma_group, groups=[], sections=[soma]}
objref soma_group
// Group: {name=all, sections=[soma]}
objref all

objref synlist

proc init() {
    topol()
    geom_nseg()
    subsets()
    subsets_inhomogeneous()
    biophys()
    biophys_inhomogeneous()
    synlist = new List()
    x = y = z = 0

}

proc set_initial_v() {
    // Taken from the initMembPotential value
    forsec all v = -70.0  // units: mV
}


proc set_initial_ion_properties() {
    // From species
}


proc topol() {
    basic_shape()

}


proc basic_shape() {

    // Section: soma, id: soma, parent: $s.parent (entry count 0/100)
    soma { pt3dclear() }
    soma { pt3dadd( 0.0, 0.0, 0.0, 10.0 ) }
    soma { pt3dadd( 0.0, 0.0, 3.1831, 10.0 ) }

}


proc subsets() { local i

    // Group: {name=soma_group, groups=[], sections=[soma]}
    soma_group = new SectionList()
    soma soma_group.append()

    // Group: {name=all, sections=[soma]}
    all = new SectionList()
    soma all.append()

}


proc subsets_inhomogeneous() {
}

proc biophys() {

    // Specific capacitance
    forsec all cm = 1.3000001  // units: uF/cm2

    // Specific axial resistance/resistivity
    forsec all Ra = 20000.0  // units: ohm cm

    // Channel densities
    forsec all {
        insert kd
        gmax_kd = 0.0  // units: S cm-2
        ek = -60.0  // units: mV
    }

    // Concentration models for species

}

func H() { // Heaviside function
    if ($1>=0) return 1
    if ($1==0) return 0.5
    return 0
}

proc biophys_inhomogeneous() {
}

proc geom_nseg() {
    // All sections not mentioned here have nseg = 1
}


proc position() { local i
    forsec all {
        for i = 0, n3d()-1 {
            pt3dchange(i, $1+x3d(i), $2+y3d(i), $3+z3d(i), diam3d(i))
        }
    }
    x = $1  y = $2  z = $3
}


endtemplate cell


